{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Calib (Svensson)",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/autoencoders/calibration.py",
      "cwd": "${workspaceFolder}",
      "env": { "PYTHONPATH": "${workspaceFolder}" },
      "console": "integratedTerminal",
      "justMyCode": true
    }
  ]
}
